"""
Sets are the collection of unique values and are immutable
repeated values will not be considered
"""

s = {56, 98, 4, 8, 23, 67, 57, 8}

s = set()  # to define empty set

print(s, type(s), len(s))

# We can't get values of set by indexing as they are unordered
